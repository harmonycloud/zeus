apiVersion: v1
kind: Service
metadata:
  name: middleware-admission-webhook-svc
  labels:
    app: middleware-admission-webhook
spec:
  ports:
  - port: 443
    targetPort: 443
  selector:
    app: middleware-admission-webhook
