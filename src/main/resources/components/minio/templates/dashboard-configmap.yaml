kind: ConfigMap
metadata:
  name: {{ include "minio.fullname" . }}-dashboard
  labels:
    grafana_dashboard: "1"
    {{- include "minio.labels" . | nindent 4 }}
apiVersion: v1
data:
{{ (.Files.Glob "dashboards/*").AsConfig | indent 2 }}